{
  "name": "@mrn/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "rl:protocol-gate": "vite-node src/headless/runFullStackProtocolGate.ts",
    "rl:protocol-gate:games": "vite-node src/headless/runProtocolGateGames.ts",
    "e2e": "playwright test",
    "e2e:live-full-game": "node scripts/live_full_game_progress_check.mjs",
    "e2e:parity": "playwright test e2e/parity.spec.ts --project=chromium --workers=1 --grep \"quick start human vs ai enters match and surfaces the first human prompt|completed engine transition shows a visible game-end state even with a stale backend turn projection|session payload initial active faces hydrate the active strip before stream events arrive|draft prompt keeps active strip hydrated before any flip events arrive|baksu draft choice stays available in final character prompt|round start shows weather and active strip before any prompt appears|purchase and mark prompts render dedicated decision cards\"",
    "e2e:human-runtime": "playwright test e2e/human_play_runtime.spec.ts --project=chromium --workers=1",
    "e2e:module-runtime": "playwright test e2e/human_play_runtime.spec.ts --project=chromium --workers=1 --grep \"human quick start surfaces turn banner and first prompt through stable ids|runtime keeps fortune cash loss cause readable in overlay and event feed|matchmaker adjacent purchase prompt labels the ability and double-price context\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^22.13.10",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^5.0.0",
    "fast-check": "^4.7.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vitest": "^3.2.4"
  }
}
