import { koLocale } from "./locales/ko";

export const DEFAULT_APP_TEXT = koLocale.app;
export const DEFAULT_LOBBY_TEXT = koLocale.lobby;
export const DEFAULT_CONNECTION_TEXT = koLocale.connection;
export const DEFAULT_BOARD_TEXT = koLocale.board;
export const DEFAULT_PLAYERS_TEXT = koLocale.players;
export const DEFAULT_TIMELINE_TEXT = koLocale.timeline;
export const DEFAULT_SITUATION_TEXT = koLocale.situation;
export const DEFAULT_PROMPT_TYPE_TEXT = koLocale.promptType;
export const DEFAULT_PROMPT_HELPER_TEXT = koLocale.promptHelper;
export const DEFAULT_EVENT_LABEL_TEXT = koLocale.eventLabel;
export const DEFAULT_STREAM_TEXT = koLocale.stream;
export const DEFAULT_THEATER_TEXT = koLocale.theater;
export const DEFAULT_TURN_STAGE_TEXT = koLocale.turnStage;
export const DEFAULT_PROMPT_TEXT = koLocale.prompt;
