# apps/web

React-based online game client.

Planned scope:

- live session board UI
- prompt decision overlay
- spectator/replay compatible rendering

Bootstrap target:

1. `cd apps/web`
2. `npm create vite@latest . -- --template react-ts`
3. `npm install`
4. `npm run dev`

