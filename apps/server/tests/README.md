# apps/server/tests

Server-side tests for the online runtime migration.

Current baseline:

- unit tests for in-memory session lifecycle service
- API route smoke tests can be added once FastAPI test dependencies are standardized

