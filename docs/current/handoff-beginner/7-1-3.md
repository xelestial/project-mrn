# 7-1-3. 로비에서 자주 보는 버그

Status: ACTIVE
Updated: 2026-05-03

상위 목차: [인수인계 문서 지도](README.md)
문서 위치: [7. 게임 로비/게임 로그](7.md) > [7-1. 게임 로비](7-1.md) > [7-1-3. 로비에서 자주 보는 버그](7-1-3.md)

방과 세션을 혼동하면 "방은 있는데 게임이 안 시작됨", "세션 토큰이 없음", "새로고침 후 연결이 끊김" 같은 문제가 생깁니다. 먼저 `room_no`, `room_member_token`, `session_id`, `session_token` 중 어느 단계의 값이 없는지 확인하세요.

## 상세 인수인계

### 이 문서에서 이해해야 할 것

이 문서는 로비/로그 관점의 `로비에서 자주 보는 버그`을 설명합니다. 로비는 게임 시작 전 좌석과 준비 상태를 관리하고, 로그는 게임 중 발생한 일을 다시 설명하는 증거입니다.

### 관련 코드와 문서

- `apps/server/src/routes/rooms.py`
- `apps/server/src/services/room_service.py`
- `apps/server/src/routes/debug.py`
- `apps/server/src/infra/game_debug_log.py`
- `apps/web/src/features/lobby/LobbyView.tsx`
- `apps/web/src/App.tsx`

### 실제 처리 흐름과 기대 값

- 이 문서의 로비 흐름: 사용자가 방을 만들고 좌석을 고르고 준비한 뒤 호스트가 시작합니다.
- 화면/요청 경로: `LobbyView.tsx` 버튼 -> `App.tsx` 핸들러 -> `sessionApi.ts` room 함수 -> `routes/rooms.py` -> `RoomService`.
- 시작 후 연결: `startRoom` 응답의 `session_id/session_token`으로 match route를 만들고 WebSocket stream을 엽니다.
- 기대 값/검증 포인트: 로비에서는 `room_no/room_token/ready`, 매치에서는 `session_id/session_token/seq`를 봅니다.

### 수정할 때 주의할 점

- 방(room)과 세션(session)을 섞어 말하면 디버깅이 어려워집니다.
- 로그는 seq, request_id, module_id, view_state 순서로 좁히세요.
- 프론트 debug log는 화면에서 일어난 일을 서버 로그와 맞추기 위한 보조 자료입니다.

### 확인 방법

- `.venv/bin/python -m pytest apps/server/tests -k "room or replay or stream" -q`
- `npm --prefix apps/web run test -- src/infra/http src/hooks --run`

### 초보자가 자주 헷갈리는 질문

- “이 값은 프론트가 계산하나요?”: 기본적으로 아니요. 규칙/권한/비공개성은 엔진과 백엔드가 결정해야 합니다.
- “화면이 맞으면 구현도 맞나요?”: 아니요. stream event, view_state, 테스트가 같은 흐름을 말해야 합니다.
- “어디부터 로그를 보나요?”: stream `seq`를 먼저 보고, 프롬프트 문제면 `request_id`, 런타임 문제면 `module_id`를 봅니다.
