# 3-7-1. Redis는 언제 쓰이나

Status: ACTIVE
Updated: 2026-05-03

상위 목차: [인수인계 문서 지도](README.md)
문서 위치: [3. 백엔드](3.md) > [3-7. Redis](3-7.md) > [3-7-1. Redis는 언제 쓰이나](3-7-1.md)

`apps/server/src/state.py`에서 `runtime_settings.redis_url`이 설정되어 있으면 Redis 저장소를 사용합니다. 설정이 없으면 JSON 파일 또는 메모리 기반 fallback을 사용합니다.

## 상세 인수인계

### 이 문서에서 이해해야 할 것

이 문서는 백엔드 관점의 `Redis는 언제 쓰이나`을 설명합니다. 백엔드는 API 요청, 엔진 실행, stream 발행, 저장소 연결, visibility projection 사이의 접착층입니다.

### 관련 코드와 문서

- `apps/server/src/app.py`
- `apps/server/src/state.py`
- `apps/server/src/routes/`
- `apps/server/src/services/`
- `apps/server/src/domain/view_state/`
- `apps/server/src/infra/redis_client.py`
- `apps/server/src/services/realtime_persistence.py`
- `apps/server/src/services/persistence.py`

### 실제 처리 흐름과 기대 값

- 이 문서의 흐름: Redis는 언제 쓰이나.
- 진입점: 서버가 여러 프로세스/재시작/복구 상황에서도 session, stream, prompt 상태를 이어야 할 때 Redis 저장소를 사용합니다.
- 내부 처리: 서비스가 이미 판정한 상태를 Redis에 저장하거나 stream replay 가능한 형태로 남깁니다.
- 기대 값/검증 포인트: Redis는 규칙을 계산하지 않고, 저장된 seq/state가 서버 메모리 상태와 어긋나지 않아야 합니다.

### 수정할 때 주의할 점

- Redis에 값이 있다는 사실과 그 값이 최신이라는 사실은 다릅니다. seq, checkpoint version, projection freshness를 같이 확인하세요.
- private 정보가 Redis canonical stream에 그대로 저장되면 안 됩니다. viewer별 projection은 배달/캐시 단계에서 만들어야 합니다.
- Redis 장애처럼 보여도 실제로는 런타임 lease, prompt replay, stale view_state 문제일 수 있습니다.

### 확인 방법

- `.venv/bin/python -m pytest apps/server/tests/test_redis_realtime_services.py -q`
- `.venv/bin/python -m pytest apps/server/tests/test_stream_service.py -q`

### 초보자가 자주 헷갈리는 질문

- “이 값은 프론트가 계산하나요?”: 기본적으로 아니요. 규칙/권한/비공개성은 엔진과 백엔드가 결정해야 합니다.
- “화면이 맞으면 구현도 맞나요?”: 아니요. stream event, view_state, 테스트가 같은 흐름을 말해야 합니다.
- “어디부터 로그를 보나요?”: stream `seq`를 먼저 보고, 프롬프트 문제면 `request_id`, 런타임 문제면 `module_id`를 봅니다.
