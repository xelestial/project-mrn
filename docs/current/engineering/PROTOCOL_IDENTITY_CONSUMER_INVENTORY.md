# Protocol Identity Consumer Inventory

Date: 2026-05-15

Source plan: `docs/current/planning/PLAN_RUNTIME_PROTOCOL_STABILITY_AND_IDENTITY.md`

This inventory exists because numeric `player_id` still has three different
meanings in the current system:

- Internal engine actor index: allowed inside engine/runtime adaptation.
- Compatibility alias: allowed at protocol/tool boundaries only while public
  identity fields are additive.
- Display/debug label input: allowed for `P1`-style labels, replay summaries,
  and audit grouping.

Do not remove numeric aliases until this inventory has no `compat alias` entries.
Removing numeric fields before that point would break known protocol consumers
instead of completing the identity migration.

## Classification

- `display`: the numeric value is used only for labels, summaries, replay rows,
  latency grouping, or audit output. It is not the public identity contract.
- `engine bridge`: the numeric value is needed to adapt a public protocol
  identity back to the current engine seat/player index.
- `compat alias`: the numeric value crosses a protocol, tool, schema, or API
  boundary for compatibility while public identity companions are introduced.
- `protocol violation`: the numeric value is being used as public identity
  without a public companion field or an explicit bridge justification.

Current `protocol violation` entries: none found.

## Consumer Inventory

| Consumer | Classification | Evidence | Removal condition |
| --- | --- | --- | --- |
| `apps/web/src/domain/stream/decisionProtocol.ts` | `compat alias` | `buildDecisionFlightKey()` now prefers `publicPromptInstanceId` before the numeric `promptInstanceId` lifecycle alias when no prompt fingerprint is present, so client duplicate-flight ownership no longer needs the numeric continuation alias when the public prompt instance companion exists. `buildDecisionMessage()` sends an explicit public/protocol string `primaryPlayerId` as the submitted top-level `player_id` when the prompt boundary provides one, ignores malformed numeric public/protocol primary fields when a public companion can repair the identity, emits `primary_player_id` plus `primary_player_id_source`, and labels a top-level numeric `player_id` as `player_id_alias_role: "legacy_compatibility_alias"` only for legacy/numeric fallback decisions. It carries `public_prompt_instance_id` next to the numeric `prompt_instance_id` lifecycle bridge, plus `legacy_player_id`, numeric `missing_player_ids`, and numeric `resume_tokens_by_player_id` companions. | Outbound decision receivers must consume public/protocol `player_id` or `primary_player_id` as primary and use numeric values only from explicit legacy fields before the numeric compatibility aliases can be removed. Numeric continuation payload fields still remain until server runtime resume no longer needs the engine bridge maps and lifecycle key. |
| `apps/web/src/headless/frontendTransportAdapter.ts` | `compat alias` | `FrontendTransportAdapter.serializeStreamMessage()` uses the shared WebSocket serializer for outbound decisions, and `apps/web/src/headless/frontendTransportAdapter.spec.ts` now verifies that a decision carrying numeric `prompt_instance_id` also preserves `public_prompt_instance_id` through transport serialization. The same final-boundary test coverage verifies public/protocol decisions keep string top-level `player_id`, `primary_player_id`, explicit legacy/public/seat/viewer companions, and no numeric alias label. The adapter still transports numeric player and prompt aliases because it mirrors the browser stream protocol exactly. | This can move out of `compat alias` only after the outbound stream decision schema and server receiver no longer accept numeric prompt/player aliases as compatibility fields. |
| `apps/web/src/hooks/useGameStream.ts` | `compat alias` | UI decision submission passes `playerId`, `primaryPlayerId`, `primaryPlayerIdSource`, `legacyPlayerId`, `publicPlayerId`, `seatId`, and `viewerId` into `buildDecisionMessage()`, so explicit public/protocol primary identity becomes the submitted top-level `player_id` even when the active prompt top-level `playerId` is still numeric. Duplicate-flight keys now prefer explicit prompt primary identity, then public/protocol identity, and no longer require a numeric legacy bridge when a public identity companion is present. They also reject numeric explicit primary ids when the declared source is `public` or `protocol`, falling back to public/protocol companions instead, while numeric ids remain accepted for legacy prompts. Rendered UI prompt actionability now compares `LocalViewerIdentity` public/protocol/viewer/seat identity against `PromptViewModel.identity` before legacy fallback. Prompt overlay header display now consumes `PromptViewModel.primaryPlayerId` instead of the legacy numeric alias. | Outbound legacy aliases and remaining engine/display selectors must stop treating numeric player fields as primary identity before this can move out of `compat alias`. |
| `apps/web/src/domain/viewer/localViewerIdentity.ts` | `engine bridge` | `LocalViewerIdentity` separates `view_commit.viewer` and join-response public/protocol companions from the numeric `legacyPlayerId`; token-derived inputs remain legacy fallback sources only. When `view_commit.viewer.player_id` is still numeric but public companions are present, the model treats that numeric value as the legacy bridge and exposes `public_player_id` as `protocolPlayerId`, matching join-result normalization. `App.tsx` now passes the merged public/protocol/viewer/seat identity into stream selector local-player surfaces, consumes selector-owned `isLocalPlayer` for match-table player cards, and resolves `effectivePlayerId` only for remaining display/engine-bridge selectors. | Remaining display/history/engine selectors must keep shrinking their numeric bridge usage until `legacyPlayerId` is needed only at the engine adapter boundary or display labels. |
| `apps/web/src/headless/HeadlessGameClient.ts` | `compat alias` | Headless decisions now send public/protocol top-level `player_id` when the active prompt exposes it directly or as explicit `PromptViewModel.identity.primaryPlayerId`, expose `HeadlessDecisionContext.identity.primaryPlayerId` to policies, and write compact decision/view traces with `primary_player_id` plus explicit legacy/public companions. When the active prompt keeps numeric top-level `player_id` as a legacy alias but carries explicit public primary identity, the outbound decision submits the public identity and preserves `legacy_player_id` as the bridge. Active and raw prompt routing now use the shared prompt identity helper against latest `view_commit.viewer` public/protocol/viewer/seat identity, treating `public_player_id` as the protocol target even when `viewer.player_id` remains a numeric compatibility alias, with a narrow numeric fallback only for legacy-only prompts. Trace events now consume explicit active-prompt `primary_player_id` plus `primary_player_id_source` before falling back to public/protocol/legacy aliases, reject numeric explicit primary ids when the declared source is `public` or `protocol`, include top-level `primary_player_id`, source, protocol, legacy, public, seat, and viewer identity fields, and label numeric trace `player_id` with `player_id_alias_role: "legacy_debug_alias"` as a legacy seat/debug alias. Duplicate suppression and retry ledgers are request-id based, not numeric-player keyed. | Outbound legacy aliases and remaining trace/debug consumers must stop depending on numeric `player_id` before the numeric alias can be renamed or removed. Trace JSONL producers now explicitly label that numeric field as debug/legacy data, so consumers should prefer `primary_player_id` and companions immediately. |
| `apps/web/src/headless/httpDecisionPolicy.ts` | `compat alias` | `HttpDecisionPolicyRequest` now consumes `HeadlessDecisionContext.identity` as the single primary identity input, normalizes malformed numeric public/protocol primary fields through public/protocol companions, and sends public/protocol top-level `player_id` when available. The nested `identity.player_id` mirrors that protocol identity. Numeric top-level `player_id` plus `player_id_alias_role: "legacy_compatibility_alias"` remains only for legacy-only policy input, while `legacy_player_id` stays as the compatibility bridge. `player_summary` now matches compact view-state player rows by explicit `legacy_player_id`, then numeric legacy `player_id`, so a public/protocol string `player_id` does not break summary attachment. | Policy consumers can use top-level or nested public/protocol `player_id` as primary identity. Remaining removal depends on consumers no longer requiring numeric `legacy_player_id` and legacy-only numeric fallback fields. |
| `apps/web/src/headless/protocolReplay.ts` | `display` | Replay rows, observations, and final player summaries now expose `primary_player_id` plus source, prefer public/protocol identity when present, reject numeric explicit primary ids when the declared source is `public` or `protocol`, and keep numeric `player_id` only as the legacy display/grouping alias for reward and rank calculations. | No protocol removal blocker while replay output remains display/training artifact data and numeric fields stay explicitly legacy/display scoped. |
| `apps/web/src/headless/fullStackProtocolHarness.ts` | `display` | The harness keeps joined seat numbers for client grouping, but join parsing now materializes the numeric client bridge only from explicit `legacy_player_id`, a numeric legacy `player_id`, or the joined seat fallback. It does not coerce a public string `player_id` into a number. Active-prompt pace, repetition, and command-latency diagnostics now prefer `active_prompt_primary_player_id` or trace `primary_player_id`, reject numeric explicit primary ids when the declared source is `public` or `protocol`, and keep numeric `player_id` only as the legacy display alias. | No protocol removal blocker if harness output remains display-only and any new operator-facing identity fields continue to use primary/public identity before legacy aliases. |
| `apps/web/src/domain/selectors/streamSelectors.ts` | `display` | Stream selectors still keep some engine/display bridges numeric. Board snapshot display now consumes explicit `marker_owner_legacy_player_id`, `owner_legacy_player_id`, and `pawn_legacy_player_ids` before raw board `*_player_id` aliases, so public/protocol marker owner, tile owner, and pawn-list fields do not erase board ownership or pawn display. Prompt and `decision_requested` actor detail fallback now consumes explicit `player_label`, `legacy_player_id`, and `seat_index` companions before numeric `player_id`, so public/protocol string `player_id` does not collapse prompt display to `-`. Related-player event detail summaries such as rent, marker transfer, bankruptcy, game end, mark outcomes, and ability suppression now consume prefixed `*_legacy_player_id` / `*_seat_index` companions before raw `*_player_id` aliases, so public/protocol related-player ids do not leak into human-facing `P#` text. Turn-history participant ids and local relevance now use the same prefixed legacy/seat companions before raw related-player aliases, so public/protocol `participants` or payload `*_player_id` strings do not drop player highlighting or `mine-critical` relevance. View-state turn-stage actors, scene situation actors, theater/core feed actors, and `mark_target` candidate slots now consume explicit `actor_legacy_player_id`, `legacy_player_id`, or `*_seat_index` companions before raw actor/player aliases, so public/protocol view-state ids do not erase active actor labels, local actor relevance, or mark-target slot mapping. Local player highlighting for derived players, marker ordering, active-character slots, and match-table player cards now accepts public/protocol/player/seat/viewer identity directly before falling back to numeric legacy ids. | No protocol removal blocker while this remains display-only and labels keep using `seat_index`/`player_label` or explicit legacy fields instead of treating numeric `player_id` as protocol primary identity. Any remaining numeric use is a display/engine bridge, not protocol primary identity, and must stay behind selector adapters until server/runtime emits only explicit legacy display companions. |
| `apps/web/src/domain/selectors/promptSelectors.ts` | `engine bridge` | `promptIdentityFromActivePromptPayload()` now consumes explicit `primary_player_id` plus `primary_player_id_source` when the server provides them, rejects numeric explicit primary ids for `public` or `protocol` source, rejects string explicit primary ids for `legacy` source, then falls back to public/protocol/legacy prompt target identity. `promptViewModelFromActivePromptPayload()` can build actionable prompt view models without a numeric `legacy_player_id` bridge when public/protocol identity is present; `PromptViewModel.primaryPlayerId` and `primaryPlayerIdSource` expose that identity for display consumers, while `PromptViewModel.playerId` is only the optional legacy display/engine bridge. `isPromptTargetedToIdentity()` compares public player, protocol player, viewer, and seat identity before legacy numeric fallback; `promptPrimaryTargetId()` and `isPromptPrimaryTarget()` keep queued-prompt target checks on that identity model instead of direct top-level `PromptViewModel.playerId` comparisons. Batch module-continuation completeness now accepts complete public-player, seat, or viewer companion missing/resume maps without requiring the numeric missing/resume maps to be exposed to the selector. `mark_target` candidates and `doctrine_relief` options now preserve explicit target legacy/public/seat/viewer companions on the parsed surface model while the numeric `target_player_id` remains the command bridge. | Remaining prompt-specific blockers are compatibility aliases plus the numeric target fields still needed by engine command submission; primary prompt target/display identity no longer depends on top-level `PromptViewModel.playerId`, prompt choice target display can read explicit companion fields materialized by runtime, selected choice payload submission is now materialized and contradiction-checked by `PromptService`, and batch prompt display no longer requires numeric continuation maps when companion maps are complete. Server runtime still owns numeric bridge materialization before `PromptService` and semantic guard validation. |
| `apps/server/src/domain/module_continuation_contract.py` | `engine bridge` | Shared module-continuation validation now owns required resume fields, simultaneous batch prompt detection, and the requirement that runtime batch prompt payloads carry numeric `missing_player_ids` plus `resume_tokens_by_player_id`. Public-player, seat, and viewer companion maps do not satisfy this runtime guard by themselves because the engine continuation still resumes by internal actor index. | This can move out of `engine bridge` only after engine simultaneous batch continuation no longer resumes through numeric actor-index maps. |
| `apps/server/src/services/runtime_service.py::_materialize_prompt_boundary_sync` / `_publish_prompt_boundary_sync` | `compat alias` | Runtime prompt materialization now enriches legal choice `value.target_player_id` payloads with authoritative `target_legacy_player_id`, `target_public_player_id`, `target_seat_id`, and `target_viewer_id` companions before creating the pending prompt. Runtime prompt publishing converts WebSocket `prompt` messages and paired `decision_requested` events to primary-only public wire payloads through `public_primary_player_wire_payload(..., omit_player_id_for_public=True)` when `public_player_id` is available, so the published public/protocol prompt boundary no longer includes top-level `player_id` and keeps the numeric seat only as explicit `legacy_player_id`. The pending prompt stored in `PromptService` still keeps numeric `player_id` and numeric target choice bridges for internal routing/command submission. Legacy-only prompt payloads remain supported as labeled numeric compatibility aliases. | This can move out of `compat alias` only after prompt lifecycle, engine continuation, decision parsing, and clients no longer require numeric `legacy_player_id` or numeric target bridge fields. |
| `apps/server/src/services/prompt_service.py` | `engine bridge` | `PromptService.create_prompt()` assigns `primary_player_id`, `primary_player_id_source`, and `player_id_alias_role` when a numeric `player_id` remains as the engine routing alias, rejects malformed explicit primary identity where source and value type disagree, and repairs through public/protocol/legacy companions before storing the pending prompt. It now delegates module/batch continuation validation to the shared module-continuation contract. `submit_decision()` copies those fields from the pending prompt into lifecycle decision records, command payloads, and nested command decision payloads, materializes the selected legal choice `value` as command/decision `choice_payload` when the submitted decision only carries `choice_id`, rejects submitted target identity companions that contradict the selected legal choice `value`, and continues to validate the numeric internal `player_id` against the pending prompt. It also rejects submitted decision public player, seat, viewer, legacy, or primary identity companions that contradict the pending prompt before command materialization, and treats a non-numeric submitted `player_id` at this internal service boundary as `player_mismatch` instead of raising during integer coercion. Numeric engine routing cannot silently override conflicting public/protocol identity. | This can move out of `engine bridge` only after accepted decision commands no longer need a numeric engine `player_id` to resume modules/runtime state. |
| `apps/server/src/services/session_service.py` | `engine bridge` | `SessionService.resolve_protocol_player_id()` is the server adapter that resolves public/protocol `player_id`, explicit `primary_player_id` plus `primary_player_id_source`, `public_player_id`, `seat_id`, `viewer_id`, and legacy numeric aliases back to the internal numeric seat id required by the remaining server/runtime boundary. Public/protocol primary ids resolve through the public player id; legacy primary ids resolve through the numeric seat bridge; malformed source/value pairs are unresolved. It evaluates all supplied identity fields as one candidate set and returns no match when public/protocol identity and numeric compatibility aliases resolve to different seats, so route handlers reject conflicting payloads through their existing mismatch path instead of silently preferring the numeric alias. | This can move out of `engine bridge` only after route handlers and downstream services no longer require an internal numeric seat id for prompt submission, authorization, and engine/runtime calls. Until then, it remains the single server-side compatibility adapter for inbound player identity. |
| `apps/server/src/domain/visibility/projector.py` | `engine bridge` | Private prompt, `decision_ack`, and private decision-event projection now match the target viewer through explicit `public_player_id`, `seat_id`, `viewer_id`, public/protocol `primary_player_id`, or public string top-level `player_id` before falling back to numeric `player_id` / `legacy_player_id`. Embedded active prompts in `view_state` use the same target matcher, so public-only prompt views can stay visible to the authorized viewer while non-target seats and spectators still lose private prompt data. | This remains an `engine bridge` while visibility scopes and legacy clients still depend on numeric `player_id` / `legacy_player_id` fallback, but private-message routing no longer requires that numeric bridge when public/seat/viewer target identity is present. |
| `apps/server/src/services/decision_gateway.py::build_decision_ack_payload` | `compat alias` | ACK payload construction now emits primary-only public payloads when `public_player_id` is available: top-level `player_id` is absent, `primary_player_id` plus source carry the protocol identity, and the internal numeric seat is preserved only as explicit `legacy_player_id`. Numeric-only ACK construction remains supported as a labeled legacy compatibility alias. | This can move out of `compat alias` only after numeric-only ACK payloads and clients depending on `legacy_player_id` are removed. |
| `packages/runtime-contracts/ws/schemas/outbound.decision.schema.json` | `compat alias` | The outbound decision schema accepts public string `player_id`, explicit `primary_player_id` plus `primary_player_id_source`, `player_id_alias_role`, and explicit public/seat/viewer companions. Top-level `player_id` is no longer mandatory when a primary/public/seat/viewer identity channel is present, but identity-less decisions are still invalid. It also explicitly defines the outbound continuation companions already used by clients and the server: `prompt_instance_id`, `public_prompt_instance_id`, frame/module/batch metadata, numeric missing/resume maps, and public-player/seat/viewer companion missing/resume maps. Numeric `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, `primary_player_id_source`, and `legacy_player_id` are present. The schema now rejects numeric `primary_player_id` whenever `primary_player_id_source` is `public` or `protocol`, and rejects string `primary_player_id` whenever the source is `legacy`. `outbound.decision.public_identity.json` freezes the public top-level identity shape, `outbound.decision.primary_identity.json` freezes the primary-only public shape without top-level `player_id`, and the numeric compatibility example remains explicit legacy evidence. | All outbound decision producers and receivers must stop depending on numeric `player_id` as the primary identity. External-AI request optionalization is tracked separately at the worker request boundary. |
| `packages/runtime-contracts/ws/schemas/inbound.prompt.schema.json` | `compat alias` | The inbound prompt schema accepts public string payload `player_id`, explicit `primary_player_id` plus source metadata, `player_id_alias_role: "legacy_compatibility_alias"`, and public companions. Top-level payload `player_id` is no longer mandatory when a primary/public/seat/viewer identity channel is present, but identity-less prompt payloads remain invalid. Numeric payload `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, `primary_player_id_source`, and `legacy_player_id` are present. The schema now rejects numeric `primary_player_id` whenever `primary_player_id_source` is `public` or `protocol`, and rejects string `primary_player_id` whenever the source is `legacy`. `inbound.prompt.public_identity.json` freezes the preferred public-primary prompt and active-view shape, `inbound.prompt.primary_identity.json` freezes the primary-only prompt shape without top-level payload `player_id`, and `inbound.prompt.movement.json` plus `inbound.prompt.trick_to_use.with_view_state.json` freeze legacy numeric prompt examples with explicit alias metadata on both the top-level prompt payload and nested active prompt view. | Prompt producers and consumers must use `primary_player_id`/public companions as primary target identity and treat numeric `player_id` only as the labeled legacy compatibility alias when present. |
| `packages/runtime-contracts/ws/schemas/inbound.decision_ack.schema.json` | `compat alias` | The inbound decision ACK schema accepts public string payload `player_id`, explicit `primary_player_id` plus source metadata, `player_id_alias_role: "legacy_compatibility_alias"`, and public companions. Top-level payload `player_id` is no longer mandatory when a primary/public/seat/viewer identity channel is present, but identity-less ACK payloads remain invalid. Numeric payload `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, `primary_player_id_source`, and `legacy_player_id` are present. The schema now rejects numeric `primary_player_id` whenever `primary_player_id_source` is `public` or `protocol`, and rejects string `primary_player_id` whenever the source is `legacy`. `inbound.decision_ack.public_identity.json` freezes the preferred public-primary ACK shape, `inbound.decision_ack.primary_identity.json` freezes the primary-only ACK shape without top-level payload `player_id`, and the legacy numeric examples remain compatibility evidence. | ACK producers and consumers must use `primary_player_id` or public companions as primary identity and treat numeric `player_id` only as the labeled legacy compatibility alias before the numeric ACK field can be renamed or removed. |
| `packages/runtime-contracts/ws/examples/inbound.event.decision_requested.external_ai.json` | `compat alias` | The external-AI `decision_requested` event example now freezes the same primary-only public wire shape as runtime prompt publishing: top-level `player_id` is absent, `primary_player_id` and `public_player_id` carry the public identity, and `legacy_player_id` keeps the numeric seat bridge. The event schema remains intentionally broad for domain event compatibility. | Event producers and consumers must use primary/public/seat/viewer companions for decision-request identity; numeric seat routing remains available only through explicit legacy companions until the prompt lifecycle bridge is removed. |
| `packages/runtime-contracts/ws/examples/sequence.decision.*.json` | `compat alias` | The decision event sequence examples now freeze runtime fanout identity enrichment for direct numeric `player_id` events and `acting_player_id` domain events. Numeric fields remain visible as compatibility/display aliases, but every direct player event carries `legacy_player_id`, `primary_player_id`, public player, seat, and viewer companions, while actor events carry the same fields with `acting_` prefixes. | These sequence examples can move out of `compat alias` only after domain fanout events stop exposing numeric player aliases and consumers use the public/seat/viewer companions for protocol identity. |
| `packages/runtime-contracts/ws/examples/inbound.event.*.with_view_state.json` | `compat alias` | The top-level `turn_start` and `player_move` event examples with embedded `view_state` now carry actor-prefixed legacy/public/seat/viewer companions beside numeric `acting_player_id`. The nested `view_state` still contains numeric actor/display ids because that subtree is selector/display evidence, not the primary protocol identity boundary. | Top-level event consumers must use actor public/seat/viewer companions before the numeric actor alias can be removed. Nested view-state numeric display fields require a separate selector/display migration decision. |
| `apps/server/src/services/external_ai_worker_service.py` | `compat alias` | The reference and priority-scored external AI worker adapters now choose `mark_target` and `doctrine_relief` targets from `preferred_target_public_player_id`, `preferred_target_seat_id`, or `preferred_target_viewer_id` when those companion fields match the legal choice `value`. They also treat multiple preferred target companions as one candidate set and ignore the preference when public/seat/viewer companions and the legacy numeric `preferred_target_player_id` resolve to different legal choices, so a numeric alias cannot silently contradict the public target preference. They still fall back to legacy numeric `preferred_target_player_id` and numeric choice/value fields for compatibility, and they echo the selected choice payload unchanged. Runtime materialization now supplies target companions for numeric target choice values before worker-visible prompts are published. | Target-choice worker policy no longer requires numeric target identity when companion fields are present, and conflicting target companions no longer get silently resolved by field priority. Numeric target choice fields still remain until downstream decision submission and engine parsing stop needing the bridge. |
| `packages/runtime-contracts/external-ai/schemas/request.schema.json` | `compat alias` | The external AI decision request schema accepts public string `player_id`, explicit `primary_player_id` plus source metadata, `player_id_alias_role: "legacy_compatibility_alias"`, and public companions. Top-level `player_id` is no longer mandatory when a request carries explicit `primary_player_id` plus source, `public_player_id`, `seat_id`, or `viewer_id`, but identity-less requests are still invalid. Numeric `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, `primary_player_id_source`, and `legacy_player_id` are present. The schema now rejects numeric `primary_player_id` whenever `primary_player_id_source` is `public` or `protocol`, and rejects string `primary_player_id` whenever the source is `legacy`. Frozen external-AI request examples now use primary-only public identity and omit top-level `player_id`; focused schema tests still prove public top-level `player_id` and labeled numeric aliases remain accepted compatibility inputs. | External worker requests must use `primary_player_id`/public companions as primary identity, with numeric `player_id` retained only as the labeled legacy compatibility alias if still needed. |
| `apps/server/src/external_ai_app.py::ExternalAiDecisionRequest` | `compat alias` | The reference worker `/decide` request model mirrors the external-AI request schema: it accepts top-level `player_id`, explicit `primary_player_id`, `public_player_id`, `seat_id`, or `viewer_id` as identity channels, rejects identity-less requests, and rejects blank identity strings. Worker decision selection still ignores player identity and uses request type, capabilities, public context, and legal choices. | This boundary only validates worker input shape. Server callback routing, prompt submission, and engine command continuation still normalize through their own adapters before numeric bridges can be removed. |
| `apps/server/src/domain/view_state/prompt_selector.py` | `compat alias` | Active prompt view-state now publishes public/protocol top-level `player_id` when the source prompt exposes `public_player_id` or a string protocol `player_id`, preserves `legacy_player_id` as the explicit numeric bridge, and emits `player_id_alias_role: "legacy_compatibility_alias"` only for legacy-only numeric top-level prompts. It still forwards numeric continuation aliases such as `missing_player_ids`, `resume_tokens_by_player_id`, and numeric `prompt_instance_id` when the source prompt includes them. `mark_target` and `doctrine_relief` surface projection preserves target legacy/public/seat/viewer companions next to the legacy numeric `target_player_id` bridge, and runtime now materializes those companions on prompt legal choices before pending/published prompt consumers see them. `apps/server/tests/test_view_state_prompt_selector.py::ViewStatePromptSelectorTests::test_build_prompt_view_state_preserves_companion_only_batch_continuation` verifies that the projection layer can preserve complete public-player, seat, and viewer continuation companions without fabricating numeric continuation aliases. | Remaining blockers are not projection-layer readability. They are server runtime resume validation, lifecycle aliases, numeric engine command bridges, and legacy-only consumers that still require numeric prompt identity. Public/protocol active prompt `player_id`, target choice companion projection, and companion-only batch projection no longer depend on unlabeled numeric inference, and `PromptService` now rejects selected-choice target identity contradictions before command materialization, but runtime still materializes numeric maps before semantic guard validation. |
| `apps/server/src/services/redis_state_inspector.py` | `display` | Redis inspector compact prompt/runtime/output rows now preserve `legacy_request_id`, `public_request_id`, `public_prompt_instance_id`, `primary_player_id`, `primary_player_id_source`, and player/seat/viewer companions alongside the numeric `prompt_instance_id` and legacy `player_id`. `apps/server/tests/test_redis_state_inspector.py::RedisStateInspectorTests::test_inspector_reports_consistent_active_prompt_state` verifies active runtime prompts plus pending/lifecycle prompt records expose the public prompt instance companion instead of showing only the numeric lifecycle alias. | No protocol removal blocker while this remains read-only operator/debug output. Numeric `prompt_instance_id` stays visible here as the runtime lifecycle bridge, but operator evidence must not force investigators to infer prompt identity from the numeric alias alone. |
| `tools/scripts/external_ai_full_stack_smoke.py` | `compat alias` | The smoke worker request and callback payload now consume explicit pending-prompt `primary_player_id` plus `primary_player_id_source` before fallback inference, reject malformed numeric public/protocol primary fields in favor of public companions, and copy pending prompt request/player/seat/viewer companions. The worker `/decide` request and server callback body omit top-level `player_id` for public/protocol identity and send `primary_player_id`, `primary_player_id_source`, and companions instead; numeric worker/callback `player_id` plus `player_id_alias_role` remains only for legacy-only prompts. The server callback route still normalizes those protocol identities into the internal numeric `PromptService` bridge. The operator result summary now also exposes `primary_player_id`, `primary_player_id_source`, and request/player/seat/viewer companions next to the raw pending `player_id`, so smoke evidence no longer presents the numeric alias as the only visible identity. | Worker request and server callback consumers can already accept primary/companion identity without top-level `player_id`; legacy-only paths must stop depending on numeric aliases before those fallback fields can be removed entirely. |
| `tools/scripts/redis_restart_smoke.py` | `compat alias` | The restart smoke can find prompts when protocol `player_id` is public and `legacy_player_id` carries the numeric bridge. Submitted decision payloads now prefer public/protocol top-level `player_id`, reject malformed numeric public/protocol primary fields in favor of public companions, preserve request/player/seat/viewer companions, and emit `primary_player_id` plus `primary_player_id_source`; numeric top-level `player_id` plus `player_id_alias_role` remains only for legacy-only prompts. | Replay prompt lookup still uses the numeric bridge for operator-selected player numbers, but WebSocket decision submission no longer needs numeric identity when public/protocol identity exists. |
| `tools/scripts/game_debug_log_audit.py` | `display` | The audit groups duplicate decisions and prompt lifecycles by `session_id`, request id, and an explicit identity key that prefers `primary_player_id`, public, protocol, viewer, or seat identity before falling back to legacy/display numeric fields. | No protocol removal blocker while audit output remains a human-readable diagnostic and numeric values are fallback legacy/display labels only. |

## Removal Gates

1. Runtime contract schemas stop requiring numeric `player_id` as the public
   identity field.
2. Browser, headless, external-AI, and smoke decision submitters can send a
   decision without a numeric public `player_id` when public identity companions
   are present; this is now regression-tested, including malformed numeric
   public/protocol primary fields that must fall back to public companions.
3. Prompt selection keeps the engine bridge at one explicit adapter boundary
   instead of requiring protocol payload consumers to infer numeric identity.
4. Replay, harness, and audit outputs either use public identity keys or mark
   numeric values as display/legacy-only.
5. This inventory is updated until no `compat alias` entries remain; only then
   can numeric alias removal be treated as a protocol cleanup instead of a
   breaking migration.
