# Protocol Identity Consumer Inventory

Date: 2026-05-14

Source plan: `docs/current/planning/PLAN_RUNTIME_PROTOCOL_STABILITY_AND_IDENTITY.md`

This inventory exists because numeric `player_id` still has three different
meanings in the current system:

- Internal engine actor index: allowed inside engine/runtime adaptation.
- Compatibility alias: allowed at protocol/tool boundaries only while public
  identity fields are additive.
- Display/debug label input: allowed for `P1`-style labels, replay summaries,
  and audit grouping.

Do not remove numeric aliases until this inventory has no `compat alias` entries.
Removing numeric fields before that point would break known protocol consumers
instead of completing the identity migration.

## Classification

- `display`: the numeric value is used only for labels, summaries, replay rows,
  latency grouping, or audit output. It is not the public identity contract.
- `engine bridge`: the numeric value is needed to adapt a public protocol
  identity back to the current engine seat/player index.
- `compat alias`: the numeric value crosses a protocol, tool, schema, or API
  boundary for compatibility while public identity companions are introduced.
- `protocol violation`: the numeric value is being used as public identity
  without a public companion field or an explicit bridge justification.

Current `protocol violation` entries: none found.

## Consumer Inventory

| Consumer | Classification | Evidence | Removal condition |
| --- | --- | --- | --- |
| `apps/web/src/domain/stream/decisionProtocol.ts` | `compat alias` | `buildDecisionMessage()` now sends an explicit public/protocol string `primaryPlayerId` as the submitted top-level `player_id` when the prompt boundary provides one, emits `primary_player_id` plus `primary_player_id_source`, and labels a top-level numeric `player_id` as `player_id_alias_role: "legacy_compatibility_alias"` only for legacy/numeric fallback decisions. It still carries `legacy_player_id`, numeric `missing_player_ids`, numeric `resume_tokens_by_player_id`, and numeric `prompt_instance_id` companions. | Outbound decision receivers must consume public/protocol `player_id` or `primary_player_id` as primary and use numeric values only from explicit legacy fields before the numeric compatibility aliases can be removed. |
| `apps/web/src/hooks/useGameStream.ts` | `compat alias` | UI decision submission passes `playerId`, `primaryPlayerId`, `primaryPlayerIdSource`, `legacyPlayerId`, `publicPlayerId`, `seatId`, and `viewerId` into `buildDecisionMessage()`, so explicit public/protocol primary identity becomes the submitted top-level `player_id` even when the active prompt top-level `playerId` is still numeric. Duplicate-flight keys now prefer explicit prompt primary identity, then public/protocol identity, and no longer require a numeric legacy bridge when a public identity companion is present, while numeric ids remain accepted for legacy prompts. Rendered UI prompt actionability now compares `LocalViewerIdentity` public/protocol/viewer/seat identity against `PromptViewModel.identity` before legacy fallback. Prompt overlay header display now consumes `PromptViewModel.primaryPlayerId` instead of the legacy numeric alias. | Outbound legacy aliases and remaining engine/display selectors must stop treating numeric player fields as primary identity before this can move out of `compat alias`. |
| `apps/web/src/domain/viewer/localViewerIdentity.ts` | `engine bridge` | `LocalViewerIdentity` separates `view_commit.viewer` public/protocol companions from the numeric `legacyPlayerId`; token and join-response inputs are treated as legacy fallback sources only. `App.tsx` uses the public/protocol/viewer/seat fields for prompt actionability and resolves `effectivePlayerId` from this model for existing display/engine-bridge selectors. | Remaining selectors must use public/protocol identity directly, leaving numeric `legacyPlayerId` only at the engine adapter boundary or display labels. |
| `apps/web/src/headless/HeadlessGameClient.ts` | `compat alias` | Headless decisions now send public/protocol top-level `player_id` when the active prompt exposes it directly or as explicit `PromptViewModel.identity.primaryPlayerId`, expose `HeadlessDecisionContext.identity.primaryPlayerId` to policies, and write compact decision/view traces with `primary_player_id` plus explicit legacy/public companions. When the active prompt keeps numeric top-level `player_id` as a legacy alias but carries explicit public primary identity, the outbound decision submits the public identity and preserves `legacy_player_id` as the bridge. Active and raw prompt routing now use the shared prompt identity helper against latest `view_commit.viewer` public/protocol/viewer/seat identity, with a narrow numeric fallback only for legacy-only prompts. Trace events now include top-level `primary_player_id`, `primary_player_id_source`, protocol, legacy, public, seat, and viewer identity fields while preserving numeric trace `player_id` as the legacy seat/debug alias. Duplicate suppression and retry ledgers are request-id based, not numeric-player keyed. | Outbound legacy aliases and trace/debug consumers must stop treating numeric `player_id` as the primary player identity before the numeric alias can move to explicit legacy/debug-only naming. |
| `apps/web/src/headless/httpDecisionPolicy.ts` | `compat alias` | `HttpDecisionPolicyRequest` now consumes `HeadlessDecisionContext.identity` as the single primary identity input and sends public/protocol top-level `player_id` when available. The nested `identity.player_id` mirrors that protocol identity. Numeric top-level `player_id` plus `player_id_alias_role: "legacy_compatibility_alias"` remains only for legacy-only policy input, while `legacy_player_id` stays as the compatibility bridge. | Policy consumers can use top-level or nested public/protocol `player_id` as primary identity. Remaining removal depends on consumers no longer requiring numeric `legacy_player_id` and legacy-only numeric fallback fields. |
| `apps/web/src/headless/protocolReplay.ts` | `display` | Replay rows, observations, and final player summaries now expose `primary_player_id` plus source, prefer public/protocol identity when present, and keep numeric `player_id` only as the legacy display/grouping alias for reward and rank calculations. | No protocol removal blocker while replay output remains display/training artifact data and numeric fields stay explicitly legacy/display scoped. |
| `apps/web/src/headless/fullStackProtocolHarness.ts` | `display` | The harness keeps joined seat numbers for client grouping, but active-prompt pace, repetition, and command-latency diagnostics now prefer `active_prompt_primary_player_id` or trace `primary_player_id` and keep numeric `player_id` only as the legacy display alias. | No protocol removal blocker if harness output remains display-only and any new operator-facing identity fields continue to use primary/public identity before legacy aliases. |
| `apps/web/src/domain/selectors/streamSelectors.ts` | `display` | Stream selectors derive actor labels, player cards, board ownership labels, and prompt actor labels from numeric player fields. | No protocol removal blocker if display labels keep using `seat_index`/`player_label` or explicit legacy fields. |
| `apps/web/src/domain/selectors/promptSelectors.ts` | `engine bridge` | `promptIdentityFromActivePromptPayload()` now consumes explicit `primary_player_id` plus `primary_player_id_source` when the server provides them, then falls back to public/protocol/legacy prompt target identity. `promptViewModelFromActivePromptPayload()` can build actionable prompt view models without a numeric `legacy_player_id` bridge when public/protocol identity is present; `PromptViewModel.primaryPlayerId` and `primaryPlayerIdSource` expose that identity for display consumers, while `PromptViewModel.playerId` is only the optional legacy display/engine bridge. `isPromptTargetedToIdentity()` compares public player, protocol player, viewer, and seat identity before legacy numeric fallback; `promptPrimaryTargetId()` and `isPromptPrimaryTarget()` keep queued-prompt target checks on that identity model instead of direct top-level `PromptViewModel.playerId` comparisons. | Remaining prompt-specific blockers are compatibility aliases and choice payloads that still carry engine-seat numeric fields; primary prompt target/display identity no longer depends on top-level `PromptViewModel.playerId`. |
| `apps/server/src/services/prompt_service.py` | `engine bridge` | `PromptService.create_prompt()` assigns `primary_player_id`, `primary_player_id_source`, and `player_id_alias_role` when a numeric `player_id` remains as the engine routing alias. `submit_decision()` copies those fields from the pending prompt into lifecycle decision records, command payloads, and nested command decision payloads, while continuing to validate the numeric internal `player_id` against the pending prompt. | This can move out of `engine bridge` only after accepted decision commands no longer need a numeric engine `player_id` to resume modules/runtime state. |
| `packages/runtime-contracts/ws/schemas/outbound.decision.schema.json` | `compat alias` | The outbound decision schema accepts public string `player_id`, `primary_player_id`, `primary_player_id_source`, `player_id_alias_role`, and explicit public companions. It also explicitly defines the outbound continuation companions already used by clients and the server: `prompt_instance_id`, `public_prompt_instance_id`, frame/module/batch metadata, numeric missing/resume maps, and public-player/seat/viewer companion missing/resume maps. Numeric `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, and `primary_player_id_source` are present. | All outbound decision producers and receivers must stop depending on numeric `player_id` as the primary identity. |
| `packages/runtime-contracts/ws/schemas/inbound.prompt.schema.json` | `compat alias` | The inbound prompt schema accepts public string payload `player_id`, explicit `primary_player_id` plus source metadata, `player_id_alias_role: "legacy_compatibility_alias"`, and public companions. Numeric payload `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, and `primary_player_id_source` are present. | Prompt producers and consumers must use `primary_player_id`/public companions as primary target identity and treat numeric `player_id` only as the labeled legacy compatibility alias when present. |
| `packages/runtime-contracts/ws/schemas/inbound.decision_ack.schema.json` | `compat alias` | The inbound decision ACK schema now requires `player_id_alias_role`, `primary_player_id`, and `primary_player_id_source` whenever payload `player_id` is numeric. ACK examples carry the same primary/public/legacy player companions as prompt and decision payloads. | ACK consumers must use `primary_player_id` or public companions as primary identity and treat numeric `player_id` only as the labeled legacy compatibility alias before the numeric ACK field can be renamed or removed. |
| `packages/runtime-contracts/external-ai/schemas/request.schema.json` | `compat alias` | The external AI decision request schema accepts public string `player_id`, explicit `primary_player_id` plus source metadata, `player_id_alias_role: "legacy_compatibility_alias"`, and public companions. Numeric `player_id` remains accepted for compatibility only when `player_id_alias_role`, `primary_player_id`, and `primary_player_id_source` are present. Frozen external-AI request examples now exercise public top-level `player_id` with explicit legacy/public/seat/viewer companions instead of presenting numeric `player_id` as the canonical worker contract. | External worker requests must use `primary_player_id`/public companions as primary identity, with numeric `player_id` retained only as the labeled legacy compatibility alias if still needed. |
| `tools/scripts/external_ai_full_stack_smoke.py` | `compat alias` | The smoke worker request and callback payload now consume explicit pending-prompt `primary_player_id` plus `primary_player_id_source` before fallback inference and copy pending prompt request/player/seat/viewer companions. The worker `/decide` request and server callback payload prefer public/protocol top-level `player_id` when available and keep `legacy_player_id` as the bridge; numeric `player_id` plus `player_id_alias_role` remains only for legacy-only prompts. | Worker/callback consumers must stop depending on numeric aliases before those fallback fields can be removed entirely. |
| `tools/scripts/redis_restart_smoke.py` | `compat alias` | The restart smoke can find prompts when protocol `player_id` is public and `legacy_player_id` carries the numeric bridge. Submitted decision payloads now prefer public/protocol top-level `player_id`, preserve request/player/seat/viewer companions, and emit `primary_player_id` plus `primary_player_id_source`; numeric top-level `player_id` plus `player_id_alias_role` remains only for legacy-only prompts. | Replay prompt lookup still uses the numeric bridge for operator-selected player numbers, but WebSocket decision submission no longer needs numeric identity when public/protocol identity exists. |
| `tools/scripts/game_debug_log_audit.py` | `display` | The audit groups duplicate decisions and prompt lifecycles by `session_id`, request id, and an explicit identity key that prefers `primary_player_id`, public, protocol, viewer, or seat identity before falling back to legacy/display numeric fields. | No protocol removal blocker while audit output remains a human-readable diagnostic and numeric values are fallback legacy/display labels only. |

## Removal Gates

1. Runtime contract schemas stop requiring numeric `player_id` as the public
   identity field.
2. Browser, headless, external-AI, and smoke decision submitters can send a
   decision without a numeric public `player_id` when public identity companions
   are present.
3. Prompt selection keeps the engine bridge at one explicit adapter boundary
   instead of requiring protocol payload consumers to infer numeric identity.
4. Replay, harness, and audit outputs either use public identity keys or mark
   numeric values as display/legacy-only.
5. This inventory is updated until no `compat alias` entries remain; only then
   can numeric alias removal be treated as a protocol cleanup instead of a
   breaking migration.
