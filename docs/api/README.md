# docs/api

REST and WebSocket API specifications.

Canonical active spec:

- `docs/api/online-game-api-spec.md`

Runtime contract examples for additive selector migration live in:

- `packages/runtime-contracts/ws/examples/`
